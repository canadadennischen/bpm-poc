/**
 * config.js — API Base URL 設定
 * 本地開發：留空 (相對路徑，由 Express 同源服務)
 * 雲端部署：改為後端 API URL，例如 'https://bpm-api.onrender.com'
 */
window.APP_CONFIG = {
  API_BASE: ''  // e.g. 'https://bpm-api.onrender.com'
};
